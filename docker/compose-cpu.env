# ARM CPU server configuration
# Remote VLM model server URL
MINERU_VLM_SERVER_URL=http://10.8.132.224:6002/v1

# API service port
MINERU_API_PORT=18000

# Pages processed per window (default: 64)
# Higher = more concurrent pages but more memory usage
MINERU_PROCESSING_WINDOW_SIZE=64
