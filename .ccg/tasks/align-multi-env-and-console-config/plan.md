# 实施计划

1. 对齐 `env.multi.example`、Compose fallback 和 Ops 容器运行时元数据。
2. 修改 Ops 服务发现：按部署角色过滤本机服务，并从 Router upstream 生成远程 API 健康检查定义。
3. 修改控制台服务页的节点数量和地址展示。
4. 修改配置分组渲染和应用对话框，使每组独立提交候选字段。
5. 增加后端服务发现与配置分组相关回归测试。
6. 执行完整验证，审查 diff，更新任务记录并提交/推送 `dev`。
